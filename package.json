{
  "name": "bulkimg-studio",
  "version": "1.0.0-beta",
  "private": true,
  "type": "module",
  "description": "Windows-first batch AI image generation studio built with Electrobun and Bun.",
  "scripts": {
    "start": "electrobun dev",
    "dev": "electrobun dev --watch",
    "build": "electrobun build --env=stable && bun run scripts/finalize-windows-build.ts stable",
    "build:canary": "electrobun build --env=canary && bun run scripts/finalize-windows-build.ts canary",
    "check": "tsc --noEmit",
    "test": "bun test"
  },
  "dependencies": {
    "@fontsource/inter": "^5.3.0",
    "@fontsource/jetbrains-mono": "^5.3.0",
    "@fontsource/manrope": "^5.3.0",
    "electrobun": "1.18.1",
    "fflate": "0.8.3",
    "lucide": "^1.28.0",
    "papaparse": "5.5.4"
  },
  "devDependencies": {
    "@types/bun": "1.3.14",
    "@types/papaparse": "5.3.16",
    "@types/three": "0.180.0",
    "rcedit": "^5.0.2",
    "typescript": "5.9.3"
  },
  "engines": {
    "bun": ">=1.3.14"
  }
}
